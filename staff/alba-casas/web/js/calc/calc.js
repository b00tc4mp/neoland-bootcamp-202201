class Calc {
  constructor(name) {
    this.name = name;
  }

  add(x, y) {
    return x + y;
  }

  minus(x, y) {
    return x - y;
  }

  mult(x, y) {
    return x * y;
  }

  divi(x, y) {
    return x / y;
  }
}
