const requiredTimeOptions = [0, 5, 10, 30, 60, 90, 120]
const requiredBudgetOptions = [0, 5, 10, 20, 30, 50, 100]
const repeatOptions = ['once', 'daily', 'weekly', 'biweekly', 'monthly', 'none']


module.exports = {
    requiredTimeOptions, 
    requiredBudgetOptions,
    repeatOptions
}