import './CreatedActions.sass'
import { Link, PlusIcon, ListActions } from '.'

export const CreatedActions = () => {

    return <>
        <h2>Mis Acciones</h2>
        <Link><PlusIcon /></Link>
        <ListActions />
    </>
}