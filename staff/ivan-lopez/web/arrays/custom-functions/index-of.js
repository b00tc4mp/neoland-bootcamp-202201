const animals = ['dog', 'cat', 'horse', 'pig', 'bird']

function indexOf(array, searchElement) {

    animals.indexOf(horse[animals]);

}

const index = animals.indexOf('horse')
console.log(index)

// expected: print horse position (2)