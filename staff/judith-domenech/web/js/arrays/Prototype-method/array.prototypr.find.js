const position = [5, 2, 4, 5, 20, 16, 1, 2, 12, 8, 130, 44];

const found = position.find(element => element > 10);

console.log(found);


/* El método find() devuelve el valor del primer elemento del array que cumple la función de prueba proporcionada. 
    20  */
