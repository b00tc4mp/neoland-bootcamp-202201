const elements = ['Fire', 'Air', 'Water'];

function join (array, value){

   const newArray = []

    for(let i = 0; i < array.length; i++){
        
       if(){
       
        newArray[index] =  array[i] + value  
       }
    
    }
    return newArray
}

console.log(join(elements, '+')) 

/* [ 'Fire' + 'Air' + 'Water' ] */


/* const elements = ['Fire', 'Air', 'Water'];

function join (array, value){

   const newArray = []

    for(let i = 0; i < array.length; i++){
        
        newArray[i] = array[i] + value
    }
    return newArray
}

console.log(join(elements, '+')) 


[ 'Fire+', 'Air+', 'Water+' ]
*/